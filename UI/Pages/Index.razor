@page "/"
@inject NavigationManager Navigation
@inject SignalRStateManager SignalRStateManager

@code
{
    protected override void OnInitialized()
    {
        Navigation.NavigateTo(SignalRStateManager.IsLoggedIn() ? "/chat" : "/login");
    }
}
